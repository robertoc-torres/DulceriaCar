[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
# Nixpacks fallback (if Docker is not used):
#   buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @dulceriacar/backend build"
#   startCommand = "bash scripts/railway-start-backend.sh"

[deploy]
healthcheckPath = "/api/healthz"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
