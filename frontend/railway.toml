[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @dulceriacar/frontend build"

[deploy]
startCommand = "pnpm --filter @dulceriacar/frontend exec serve dist -s -l ${PORT:-3000}"
restartPolicyType = "on_failure"
